import React from 'react';
import { Link } from 'react-router';

const App = ({ children }) => (
  <div className="main">
    <header>
        
    </header>
    {children}
  </div>
);

export default App;
